# vim: ft=neomuttrc
source "$XDG_CONFIG_HOME/neomutt/credentials/ufrgs.creds"

set folder = "imaps://imap.ufrgs.br/"

set spool_file = +INBOX
set record = +Sent
set postponed = +Drafts
set trash = +Trash

set header_cache = $XDG_CACHE_HOME/neomutt/ufrgs/header
set message_cache_dir = $XDG_CACHE_HOME/neomutt/ufrgs/message
